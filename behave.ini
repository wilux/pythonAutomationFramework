[behave]
one_background: True
persist_mode = feature
color = yes
stdout_capture = no
stderr_capture = no
log_capture = no
format = allure
junit=true

[behave.formatters]
allure   = allure_behave.formatter:AllureFormatter